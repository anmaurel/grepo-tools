version: '3.7'
services:
    node-server:
        container_name: grepo-tools_server
        build: ./
        ports:
            - 9000:9000
        networks:
            default:
                ipv4_address: 192.168.1.86
        restart: on-failure
        
networks:
    default:
        external:
            name: macvlan